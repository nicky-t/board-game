import { createMuiTheme } from "@material-ui/core";
const theme = createMuiTheme({
  palette: {
    primary: {
      light: "#baffff",
      main: "#84ffff",
      dark: "#4bcbcc",
      contrastText: "#000000",
    },
    secondary: {
      light: "#ffffff",
      main: "#e1f5fe",
      dark: "#afc2cb",
      contrastText: "#000000",
    },
  },
});
export default theme;
